@page "/"
@page "/controlCenter"
@inject NavigationManager Nav

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (!firstRender) return;

        // ✅ 先固定導到預設 Home 子頁
        Nav.NavigateTo("/controlCenter/label-engine", replace: true);
    }
}

